/* Mostre o produto interno de dois vetores. 
Esta função deve preencher dois vetores X e Y com os dados do teclado.
Exemplo para vetores que possuem 4 elementos

X = (1,2,3,4) Y = (4,3,2,1) -> X.Y = 1*4 + 2*3 + 3*2 + 4*1 = 20 */

#include <stdio.h>
#include <locale.h>

int main(){
printf sizeof(int);
    return 0;
}