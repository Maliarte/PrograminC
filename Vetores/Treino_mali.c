#include <stdio.h>
#define TAM 5 //Declarar um vetor envolve definir um tamanho; arnazena valores simultaneamente

int main(void) {
int vetor[TAM]={1,2,3,4,5}; //Atribuição para vetores só na def do mesmo
int indice=0;

    while(indice < 5){
        printf("\n%d",vetor[indice]); //acessa a posição do vetor ou seja visita
        indice++;
    }
v[4]=888;
v[2]=v[4]+8;

return 0;
}