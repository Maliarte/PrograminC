/* Alguns cadeados usam aneis rotativos numericos, ao envés de chave.
Existe um numero que deve ser selecionado nos aneis numericos
para abrir o cadeado. Vamos chamar este numero de chave numerica.
Suponha que um tal cadeado trabalha com numeros de 5 digitos.
Por ex: 23478 é uma chave possivel. Quantas possibilidades de chave numerica
existem?

*/

#include <stdio.h>
void validaDigitos( int digitos)
if (digitos //Como fazer para contabilizar a quantidade de vezes que o usuario entrou com os digitos, de forma que, se este inserir 6 digitos o programa retorna com erro?


int main(){
    int digitos;
    printf("Defina a senha para o cadeado de 5 DIGITOS ");
    contaDigitos(digitos)
    scanf("%d", &digitos); 
    possibilidades= 10*10*10*10*10;

    return 0;
}